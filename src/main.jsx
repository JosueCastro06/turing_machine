import React from 'react'
import ReactDOM from 'react-dom/client'
import { AppQuantum } from './AppQuantum'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppQuantum />
  </React.StrictMode>,
)
